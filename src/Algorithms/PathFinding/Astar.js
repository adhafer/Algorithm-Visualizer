import { get_distance } from "../AlgoHelper"

export default function Astar({array, height, width, startCell, endCell}) {
    get_distance(endCell, startCell);
// To implement!
    return (
    <div>Astar</div>
  )
}
